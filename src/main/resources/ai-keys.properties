# API keys for available AI providers
# Specify the key for each provider you wish to use.
# The selected provider can be changed via this file or the AI_PROVIDER environment variable.
openai=
anthropic=
grok=
gemini=
ollama=
lmstudio=
# provider defines which service to use by default if AI_PROVIDER is not set
provider=openai
